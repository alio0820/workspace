1.由于school表设置了CASCADE，因此必须先删除school才能删除person，如果没有设置为CASCADE，则可以先删除person
  但是只要是有外键就不能同时从可视化工具导入
      只有是有中文字符就不能从DOS   工具导入
2.person插入成功了school才能插入

3.导入的时候可以导入包含中文的数据库表，但是在控制台执行的时候就不行，而且不能修改导出的sql文件，否则无法导入